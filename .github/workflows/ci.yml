name: CI
on: [pull_request, push]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          test -s manifests/versions.yaml
          bash -n bootstrap/bootstrap-ubuntu.sh
          bash -n scripts/healthcheck.sh

